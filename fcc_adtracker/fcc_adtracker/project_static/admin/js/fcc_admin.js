/*
$(document).ready(function() {
    $('.typeahead').each(function() {
        // var options = $(this).children('option').map(function(){ if ($(this).val() !== "") {return $(this).val();} else { return null;}}).toArray();
        // var current_val = $(this).find('option:selected').val();
        // console.log(current_val);
        // var id = $('.typeahead').attr('id');
        // console.log(id);
        // var lookahead_input = $('<input id="' + id + '" type="text" value="" autocomplete="off" />');
        // $(this).replaceWith(lookahead_input);
        // if (current_val !== "") $(lookahead_input).val(current_val);
        $(this).typeahead();
        
    });
});
*/
